{:itonami.blueprint/id "cloud-itonami-iso3166-isl"
 :itonami.blueprint/name "Independent Public-Sector Market-Entry & Procurement Compliance Service — Iceland"
 :itonami.blueprint/iso3166 "ISL"
 :itonami.blueprint/domain :public-sector/market-entry-compliance
 :itonami.blueprint/license "AGPL-3.0-or-later"
 :itonami.blueprint/social-impact [:eea-single-market-access :public-spend-transparency :cross-border-friction-reduction]
 :itonami.blueprint/governor :market-entry-compliance-governor
 :itonami.blueprint/robotics false
 :itonami.blueprint/required-technologies [:identity :forms :dmn :bpmn :audit-ledger]
 :itonami.blueprint/status :public-oss
 :itonami.blueprint/maturity :implemented
 :itonami.blueprint/implemented-slice
 "src/marketentry/* (langgraph-clj StateGraph actor, compiled via
 `operation/build` -> `g/state-graph` -> `g/compile-graph` with
 `interrupt-before #{:request-approval}`): facts.cljc (official
 spec-basis catalog -- Fjársýslan/FMA as the CURRENT central
 procurement authority, explicitly NOT the decommissioned Ríkiskaup;
 Public Procurement Act 120/2016; Act No. 34/1991 sector-restriction
 table), governor.cljc (Market-Entry Compliance Governor -- spec-basis
 HARD check, evidence-incomplete HARD check, `:stale-procurement-
 authority` HARD check (flagship currency check catching an LLM citing
 the decommissioned Ríkiskaup), `:vsk-unregistered` / `:vat-agent-
 missing` HARD checks (VSK/VAT registration + non-resident agent),
 `:nonresident-clearance-missing` HARD check (Act No. 34/1991
 EEA-vs-non-EEA gate, general default for energy/aviation/real-estate),
 `:fisheries-cap-exceeded` HARD check (the ONE sector where EEA-
 established status does NOT waive the ownership cap -- independently
 recomputed, never trusted off a flag), engagement-fee-mismatch
 independent recompute, confidence-floor/actuation gate, plus
 double-draft/double-submit guards), store.cljc (MemStore + DatomicStore
 via `io.github.kotoba-lang/langchain-store`
 (`ls/enc`/`ls/dec*`/`ls/read-stream`/`ls/append-blob!`) instead of a
 hand-rolled EDN-blob codec), registry.cljc (pure-function
 filing-draft/filing-submit record construction + fee recompute),
 marketentryllm.cljc (mock MarketEntry Advisor, with injectable
 `:no-spec?`/`:stale-authority?` failure modes for governor-contract
 testing), operation.cljc (intake -> advise -> govern -> decide ->
 [request-approval ->] commit/hold StateGraph, `store/append-ledger!`
 called from both the `:commit` and `:hold` nodes), phase.cljc (0->3
 rollout, `:filing/draft`/`:filing/submit` permanently excluded from
 every phase's `:auto` set). Plus src/statute/facts.cljc (general-law
 compliance catalog, ADR-2607141700 `cloud-itonami-compliance-fact-
 federation` -- orthogonal to marketentry.facts's procurement-only
 scope: Lög um einkahlutafélög/Act on Private Limited Companies, Law
 No. 138/1994; Lög um persónuvernd og vinnslu persónuupplýsinga/Data
 Protection Act implementing GDPR, Law No. 90/2018; Lög um aðbúnað,
 hollustuhætti og öryggi á vinnustöðum/Act on Working Environment,
 Health and Safety in Workplaces, Law No. 46/1980 -- all three
 independently WebFetch-verified against althingi.is, none
 duplicating or contradicting the Fjársýslan/Ríkiskaup currency-trap
 fact `marketentry.facts` already owns), schema/statute.edn (DataScript
 schema) and data/datascript-tx.edn (derived tx-data), mirroring the
 cloud-itonami-iso3166-nor/-swe/-dnk/-fin sibling convention. 46 tests
 / 149 assertions passing (governor contract + store parity + facts +
 phase + registry + statute, plus this repo's separate `culture` domain
 namespace, left untouched), clj-kondo clean (0 errors, 9 non-fatal
 unused-binding/unused-require warnings, same shape as sibling iso3166
 actors and unrelated to this change)."}
